[alias]
st = status -sb
lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative --all
ch = checkout
co = checkout
df = diff
cp = cherry-pick
br = branch
get = !git fetch origin &&  git pull

[core]
excludesfile = /Users/senders/.gitignore_global
	autocrlf = input
[user]
name = Stephen Enders
email = "PUT YOUR MFA ENABLED  PASS HERE"
[color]
        diff = auto
        status = auto
        grep = auto
        interactive = auto
        branch = auto
